import { TemplatefilterPipe } from './templatefilter.pipe';

describe('TemplatefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TemplatefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
